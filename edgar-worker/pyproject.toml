[project]
name = "edgar-worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "availe", email = "94802525+availe@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
edgar-worker = "edgar_worker:main"

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
